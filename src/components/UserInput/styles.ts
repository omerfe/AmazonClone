import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    row: {
        marginVertical: 5,
    },
    label: {
        fontWeight: 'bold'
    },
    input: {
        backgroundColor: '#fff',
        height: 40,
        marginVertical: 5,
        borderWidth: 1,
        borderColor: '#d1d1d1',
        borderRadius: 2,
        padding: 5
    },
});

export default styles;